// Smart Asset Manager - 기본 오프라인 캐싱 서비스 워커
// index.html(자산관리.html)과 반드시 같은 폴더에 있어야 하며, HTTPS(또는 localhost)로 호스팅되어야
// 브라우저가 등록을 허용한다(file:// 로컬 실행에서는 등록 자체가 불가능 - 웹 표준 보안 정책).

const CACHE_NAME = 'smart-asset-manager-v186'; // [자산 역할(포지션) 분류 + 소유자별(신랑/와이프) 독립
// 포트폴리오 운영] 일반계좈 자산에 공격수/미드필더/수비수/코어자산 역할을 지정할 수 있게 됐고(자산
// 등록/거래내역/엑셀), "포트폴리오 구성" 탭의 목표 비중·"미래 예측" 탭의 월적립금이 전부 신랑/와이프
// 완전 독립 구조(Option B)로 재편됐다(기존 값은 두 사람 모두에게 복사되어 시작) - 실행 가이드도
// 가구 통합형 없이 신랑/와이프 각자 독립 아코디언으로 분리됐다. 목표 비중/월적립금 등록 시 미보유
// 종목도 종목 마스터 DB 전체를 대상으로 검색해 추가하고 역할을 바로 태깅할 수 있다(실제 자산 집계와는
// 완전히 격리된 별도 메타데이터). 새 "포지션별 비중 분석" 카드(부부 합산/개별 보기)와, 미래 예측 탭
// 최하단에 가구 합산 포트폴리오 몬테카를로 시뮬레이션(GBM, 1,000회, P10/P50/P90 밴드 차트) 섹션이 추가됐다.
// 헤더가 [계좈명][주기][금액][기간] 한 줄에 다 들어가다 보니 "기간" 칸이 단위 표시 없이 숫자만 보여
// 눈에 잘 안 띄었다(사용자 신고 - "적립기간도 넣을 수 있어야 하네요", 실제로는 이미 있었지만 안 보임).
// 헤더를 2줄로 나누고(1줄: 계좈명+주기, 2줄: 금액 원 + 기간 년) 금액/기간 각각에 "원"/"년" 단위를
// 붙여 명확하게 했다.
// v183에서 시도한 "자동 채움" 방식이 여전히 부족했다(사용자 신고 - "IRP/ISA 각각 금액/기간을
// 어떻게 넣으라는거냐"): 위쪽 적립 설정 목록과 아래쪽 계좌별 종목 배분이 여전히 별개 UI였다. 이제
// 아예 하나로 합쳐 소유자(신랑/와이프)별로, 실제 보유 계좈종류(IRP/ISA 등)마다 카드 하나에
// [계좈명 + 납입주기 + 금액 + 기간] + 그 계좈 보유 종목별 배분 비중을 함께 보여준다 - 계좈종류를
// 더 이상 직접 입력하지 않으므로(보유 종목에서 자동 결정) "계좈 추가/삭제" 버튼 자체가 없어졌고,
// 새 절세계좈에 종목을 사면 다음에 팝업을 열 때 카드가 자동으로 하나 더 나타난다.
// [예전] [버그 수정 - 절세계좌 적립 설정, 보유 계좈종류 자동 채움]
// [적립설정] 팝업의 적립 설정 목록이 완전히 빈 칸으로만 시작해서, 이미 "계좈별·종목별 배분"에 IRP/ISA
// 등 실제 보유 계좈종류가 보이는데도 위쪽에서는 그 이름을 오타 없이 직접 타이핑해야만 서로 연결되는
// 문제가 있었다(사용자 신고 - "IRP/ISA 각각 어떻게 넣으라는거냐"). 팝업을 열 때 이미 보유 중인
// 계좈종류가 있고 적립 설정이 비어있으면 계좈종류 칸을 미리 채운 행을 자동으로 만들어준다(js/05
// seedTaxAdvantagedContributionFromHoldings) - 사용자는 금액·기간만 입력하면 된다.
// 엑셀 "덮어쓰기" 업로드가 매번 모든 자산에 새 id를 발급하는데(엑셀엔 id 컬럼이 없음), 소급 이력 채우기
// (backfillAllHoldingsDailyPnlHistory)가 "이미 채웠는지"를 그 휘발성 id로 판단해서, 같은 포트폴리오를
// 재업로드할 때마다 최근 1년치 일간손익(dailySnapshots)이 매번 다시 합산되어 2배·3배로 부풀려지는
// 버그가 있었다(사용자 실측 신고 - 재업로드 2번으로 정확히 2배 확인). 판정 기준을 asset.id 대신
// "소유자+계좌구분+티커" 안정적인 지문으로 바꿔 재발을 막고(js/11 getBackfillFingerprint, js/12
// applyRemoteScalarFields도 동일 기준으로 통일), 이미 중복 누적된 기존 데이터는 1회성 마이그레이션으로
// 최근 과거 스냅샷을 지우고 실제 종가 이력으로 깨끗하게 다시 채운다(js/01
// remediateDuplicatedDailySnapshotHistory - 오늘 날짜 실시간 기록은 건드리지 않음).
// [적립설정] 팝업이 소유자 단위로 딱 하나의 월 적립액·적립기간만 설정하던 방식에서 벗어나, 소유자 안에서
// [계좌 추가]로 IRP/ISA/연금저축 등을 원하는 만큼 등록하고 계좌마다 독립적인 납입주기(매월/매년)·금액·
// 기간을 설정할 수 있다(state.projection.taxAdvantagedPlan.contributionByOwnerAccount, js/05). 계산
// 엔진(simulateTaxAdvantagedOwnerGrowth)이 계좌마다 다른 시점에 납입을 멈추고 그 이후엔 그 계좌 몫만
// 복리로 계속 성장하도록 정확히 반영한다 - 연납(매년 1회)은 새 computeFutureValueAnnual로, 월납은
// 기존 computeFutureValue로 계산하고 납입 종료 후 유휴 성장 구간은 이 앱의 기존 관행대로 항상 월복리로
// 계산한다. 계좌별 설정을 하나도 등록하지 않은 소유자는 예전 monthlyByOwner/yearsByOwner(owner 전체
// 단일 풀) 기준으로 그대로 계산돼 기존 데이터·동작이 100% 보존된다(하위호환 폴백).
// 바로 전 버전(v179)에서 SPYM/QQQM 키를 S&P500/NASDAQ로 바꿨는데, 이미 기기에 저장돼 있던 옛 키
// (SPYM/QQQM) 항목이 자동으로 사라지지 않아 "수익률 관리" 팝업에 새 키와 나란히 중복으로 남는
// 문제가 있었다(customScenarioRates와 자산의 rateMatchOverride 둘 다 해당) - loadState()에 1회성
// 마이그레이션을 추가해 새 키가 없으면 옛 항목을 그대로 옮기고, 새 키가 이미 있으면(재업로드 등으로
// 이미 새로 등록됨) 옛 항목을 버리도록(새 값 우선) 고쳤다.
// 사용자가 엑셀 1시트 대표매칭 칸을 이미 "S&P500"/"NSADAQ"로 직접 편집해 쓰고 있는 것을 확인하고,
// 시스템 공식 키 이름 자체를 SPYM->S&P500, QQQM->NASDAQ로 바꿨다(요청 반영). 실제로 SPYM/QQQM
// 티커를 보유한 자산은 TICKER_RATE_KEY_ALIAS(js/05)로 여전히 정확히 매칭된다 - 이름이 바뀐 건
// "대표매칭 키" 개념일 뿐, 실제 종목 티커/가격 조회 로직은 전혀 안 건드렸다.
// "수익률 관리"에 등록한 키워드가 종목명에 포함되면(예: CASH="현금,달러") 카테고리 캐치올(현금=항상
// 0%)이나 지역 대표지수 폴백보다 우선해서 그 키의 등록 수익률을 자동 적용한다(getCustomKeywordRateKey,
// js/05) - 예전엔 QQQM/SCHD/SPYM 3개 ETF에만 하드코딩된 키워드 매칭이 있었는데, 이제 어떤 등록 키든
// (BOND.STOCK="채권혼합", CASH="현금,달러", GOLD="금" 등) 사용자가 직접 키워드를 추가할 수 있다.
// "수익률 관리" 팝업에 종목별 키워드 입력칸을 추가했고, 엑셀 2시트에도 "키워드(쉼표로 구분)" 컬럼이
// 추가됐다(내려받아 채워서 재업로드하면 바로 반영). 부동산 카테고리 자동분류 키워드에 '빌라'도 추가.
// 1) getSystemDefaultRate(js/05) - BOND/부동산/KOSPI/프리셋 티커 외의 "알 수 없는 키"를 조회하면
//    undefined를 그대로 반환해 화면엔 "0%로 리셋"된 것처럼 보일 수 있었다 - 실제 계산 경로
//    (resolveProjectionRateForKey)와 동일하게 지역별 대표지수로 대체하도록 통일했다(요구사항 4, SSOT).
// 2) getScenarioRateDisplayRows(js/05) - 실제 계산에 쓰이는 "활성 키"(getActiveScenarioRateKeys)인데도
//    시스템 기본 목록에도 사용자 등록 목록(customScenarioRates)에도 없는 "고아 키"는 화면에서 완전히
//    빠졌다 - findLabelForRateKey로 보유자산/리밸런싱목표/적립배분에서 이름을 찾아 마지막 3번째 그룹으로
//    채워 넣어, 이제 어떤 활성 키도 누락 없이 전부 보이고 그 자리에서 바로 고칠 수 있다.
// 3) 거래내역 등록/수정 팝업의 "대표 추종 수익률 종목" 입력칸을 자유 입력 텍스트에서 셀렉트 드롭다운으로
//    바꿨다 - [수익률 관리] 팝업과 동일한 목록에서 고르게 하고, 이미 등록된 값이 목록에 없는 예외
//    상황(과거 오타 등)은 지우지 않고 "현재값" 옵션으로 보존해 다른 필드를 고치다 실수로 값이 날아가는
//    일이 없게 했다.
// 4) [코스닥/코스피 자동판별 - 버그 수정] 국내 상장 종목은 전용 매핑이 없으면 무조건 'KOSPI' 하나로만
//    폴백했다 - 코스닥 상장 종목(예: 파크시스템즈)도 코스피 지수 수익률을 그대로 썼고, "수익률 관리"에
//    'KOSDAQ' 키를 직접 등록해도 실제 계산엔 전혀 반영되지 않았다. 티커 접미사(.KQ)로 코스닥 상장 여부를
//    구분해 전용 'KOSDAQ' 대표 키로 매칭되도록 getProjectionAssetGroupKey/resolveTickerToRateKey/
//    getTargetProjectionRate/resolveProjectionRateForKey/getEffectiveIndexRate를 모두 고쳤다.
// index.html·js/05·06이 바뀌었으므로 v175->v177로 올려 PWA가 캐시된 예전 버전을 버리고 새로 받아오게 한다.
// 1) "자산관리" 엑셀 다운로드가 state.assets를 필터링 없이 그대로 내보내, 전량 매도(수량 0)되거나
//    가격 미입력(수량은 있지만 평가금액 0)인 유령 자산이 계속 목록에 남아있었다 - 실제 평가금액
//    (calcRow(a).curAmount)이 0이 아닌 자산만 내보내도록 필터를 추가했다(포트폴리오 구성 탭의
//    리밸런싱 가이드에 이미 적용된 것과 동일한 기준).
// 2) 대시보드 Top5 보유종목도 같은 이유로 동일한 기준(평가금액 0원 제외)으로 업그레이드했다 - 예전엔
//    수량만 확인해 가격 미입력 자산이 순위 후보에 낄 여지가 있었다.
// 1) [비중조절] 팝업의 '주식' 캐치올 하위 [보유 주식 종목 선택](selectedStocks)이 미래예측 계산에서
//    무시되던 버그를 고쳤다 - computeRegionWeightedRate/computeTargetWeightedAvgRate가 이제 종목별
//    실행 가이드와 동일한 expandRebalanceTargetsForComputation을 함께 써서, 캐치올 안에 지정한 개별
//    종목의 수익률이 가중평균 복리 계산에 정확히 반영된다(실측 검증 완료).
// 2) "수익률 관리" 팝업 목록을 하드코딩된 12개 고정 목록에서 동적 필터링으로 바꿨다 - 일반계좌+절세
//    계좌 보유 종목, 포트폴리오 구성 목표(캐치올 세부 종목 포함), 월적립금/절세계좌 적립 배분에 실제로
//    쓰이는 대표 키만 시스템 기본 상품 중에서 보여주고, 사용자가 직접 등록한 종목(모달 추가 또는 엑셀
//    두 번째 시트)은 아직 미보유라도 계속 표시한다.
// 3) 거래내역 등록/수정 팝업에 "대표 추종 수익률 종목" 입력칸을 추가했다 - 신규 매수 등록 시점에
//    바로 대표 매칭(rateMatchOverride)을 지정할 수 있다.
// 4) 자산관리 엑셀을 멀티 시트로 확장했다 - 두 번째 시트("수익률 관리 기준")에 지금 쓰이는 모든 대표
//    종목의 보수/일반/긍정 기대수익률을 함께 내려받고, 이 시트를 고쳐서 재업로드하면
//    state.projection.customScenarioRates에 즉시 반영된다(신규 종목 추가도 가능).
// 1) [적립설정] 팝업을 [소유자] → [계좌 종류(ISA/IRP/연금저축)] → [보유 종목] 계층으로 확장했다 -
//    state.projection.taxAdvantagedPlan.allocationByOwner에 (계좌,종목,비중%) 목록을 저장한다.
// 2) 절세계좌 복리 계산(simulateTaxAdvantagedOwnerGrowth)을 일반계좌와 동일한 대표 매칭 로직
//    (getProjectionAssetGroupKey)으로 재작성했다 - 원금은 실제 보유 종목별로, 배분된 적립금은 배분
//    종목별로 각자의 대표 수익률로 독립 복리 성장하고, 배분되지 않은 나머지만 기존 위험:안전 70:30
//    폴백을 쓴다. "수익률 관리" 오버라이드도 그대로 반영된다(실측 검증 완료, diff=0).
// 3) 자산에 rateMatchOverride 필드를 추가했다 - 엑셀 내보내기에 "대표매칭(수익률연동키)" 컬럼을 추가해
//    지금 어떤 대표 상품으로 계산되는지 보여주고, 이 셀을 고쳐서 다시 업로드하면 그 값이 최우선
//    반영된다(getProjectionAssetGroupKey). JSON 백업/가족 동기화에도 이 필드를 포함해 유실을 막았다.
// 4) 월 적립금 입력칸을 메인 화면(포트폴리오 기준 카드)에서 없애고 [월적립금 설정] 팝업 안으로
//    옮겼다 - 메인 화면엔 읽기 전용 요약 배지만 남는다(updateMonthlyContributionSummary).
// 1) 최상위 탭 "리밸런싱/자산예측"을 "포트폴리오/자산예측"으로 개명했다(포트폴리오 구성/미래 예측
//    2개 서브탭을 묶는 상위 탭 - 서브탭 이름은 이미 이전 작업에서 정리됨). 라우팅은 data-tab 속성과
//    state.activeTab만으로 동작해 표시 문구와 무관하므로 안전하게 이름만 바꿨다.

// 1) "포트폴리오 기준" 카드 상단의 "※ 절세계좌... 시뮬레이션합니다." 안내 문구를 삭제했다.
// 2) 모바일에서 월 적립금 입력/[월적립금 설정]/[수익률 관리]가 세로로 꺾이던 문제를 고쳐 항상 한 줄에
//    나란히 있게 했다(flex-nowrap + overflow-x-auto, 폭이 부족하면 가로 스크롤로 대응).
// 3) "목표 비중 적용(일반적) - 상품별 비중" 카드를 완전히 삭제했다(포트폴리오 구성 탭과 중복 정보).
// 4) 목표배분·보수적/일반적/긍정적 요약 카드에서 "15년 후/20년 후 예상자산" 표시를 없애고 기대수익률
//    까지만 보여주도록 간소화했다.
// 5) "절세계좌 현황" 카드에서 [전체/신랑/와이프] 필터를 없애고 항상 가구 합계로 보여주며, 대신 카드
//    하단에 신랑/와이프 세부 현황을 아코디언 2개로 접어뒀다 - 다른 탭으로 이동했다 돌아오면 자동으로
//    닫힌다(detailCardAccordionOpen의 범용 초기화 순회에 편입돼 별도 배선 없이 동작).
// 1) "총금융투자현황" 탭을 "금융투자현황"으로, "투자세부" 탭을 "총자산현황"으로 개명했다.
// 2) "총자산현황" 탭 아이콘을 list-tree에서 landmark(자산/건물을 상징)로 교체했다 - 부동산·현금 등
//    포괄적 자산을 아우른다는 새 이름의 의미에 맞춘다.
// 3) 탭 라우팅은 data-tab 속성과 state.activeTab만으로 동작해 표시 문구와 무관하므로(switchTab, js/03)
//    이름을 바꿔도 에러 없이 안전하다는 것을 확인했다(라이브 테스트로 탭 전환 검증 완료).
// 1) simulateTaxAdvantagedGrowth()가 SCENARIO_RATE_PRESETS 시스템 기본값을 직접 참조해, "수익률 관리"
//    모달에서 KOSPI/채권 수익률을 고쳐도 절세계좌 시뮬레이션에는 전혀 반영되지 않던 버그를 고쳤다 -
//    getEffectiveIndexRate/getReferenceRate로 바꿔 일반계좌와 동일하게 오버라이드를 최우선 적용한다.
// 2) 부동산 수익률을 "수익률 관리" 목록(SCENARIO_RATE_BASE_ROWS)에 복원했다 - 한때 부동산을 예측
//    계산에서 완전히 뺐을 때 함께 지웠으나, 이후 "시나리오별 총자산" 카드가 부동산을 다시 계산에
//    포함시키면서 정작 조정할 UI가 없는 상태로 남아있었다. 계산부(updateProjection)도
//    getReferenceRate('부동산', ...)를 거치도록 바꿔 오버라이드가 실제로 반영되게 했다.
// 3) [월적립금 설정](종목별 배분)은 절세계좌/부동산의 구조적 특성(각자 별도 적립 시스템 또는 적립
//    개념 자체가 없음)상 의도적으로 그대로 별개로 둔다(사용자 확인됨).
// 1) "일반계좌 현황" 섹션 타이틀을 "포트폴리오 기준"으로 개명했다.
// 2) 월 적립금 입력 옆에 [월적립금 설정] 버튼을 신설했다 - 새로 여는 모달에서 매월 적립금이 투입될
//    종목을 직접 검색·선택하고 배분 비중(%)을 지정할 수 있다(state.projection.
//    monthlyContributionAllocation에 저장). "포트폴리오 구성" 탭의 목표 비중(리밸런싱용)과는 완전히
//    별개의 설정이며, 배분되지 않은 나머지 비중은 예전처럼 국내/해외 목표 비중대로 계산된다(빈 배분 =
//    이전 동작과 완전히 동일 - 실측 검증 완료).
// 3) 절세계좌 현황 카드의 [적립 예상] 버튼을 [적립설정]으로 개명했다.
// 4) simulateRebalancedPreset()을 "원금(지역 블렌디드) + 월 적립금(배분 종목별 독립 계산 + 나머지
//    지역 블렌디드)"으로 분리해 위 배분 설정이 실제 복리 시뮬레이션에 정확히 반영되도록 했다.
// 1) 포트폴리오 구성 탭(및 그 전용 모달 3개, js/04-rebalancing.js)에서 "리밸런싱"이라는 단어를 화면
//    문구/배지/엑셀 다운로드/토스트/파일명까지 전부 걷어내고 "실행 가이드"/"조정 필요금액"/"구성 제외
//    자산"/"목표 비중 계산" 등으로 정리했다(미래예측 탭에 이어 두 번째 정리 - 요청 반영).
// 2) "절세계좌 현황" [적립 예상] 팝업을 개편했다 - 신랑/와이프가 공유하던 단일 적립 기간(년) 입력을
//    각자의 [월 적립 예상액]+[적립 기간(년)] 묶음으로 분리해, 두 사람이 서로 다른 목표 시점을 가질 수
//    있게 했다(state.projection.taxAdvantagedPlan.yearsByOwner). 결과 표/시나리오별 총자산 그래프
//    모두 각자의 적립 기간이 끝나면 그 이후로는 추가 적립 없이 복리만 지속되는 것으로 정확히 계산한다.
// 3) [버그 수정] JSON 백업 복원/가족 동기화 pull 시 state.projection.taxAdvantagedPlan 필드가 통째로
//    누락되어 그 직후 미래예측 탭이 TypeError로 죽던 문제를 고쳤다(loadState와 동일한 정규화 로직 재사용).
// 1) "시장현황 & 매크로 브리핑" 카드에 달러인덱스(USD Index, Yahoo DX-Y.NYB) 지표를 새로 추가했다.
//    1행을 4개→5개로 늘려 금 시세 바로 다음(VIX/원달러/美10년물/금시세/달러인덱스)에 배치했고, 2행
//    (코스피/코스닥/S&P500/나스닥/다우)은 그대로 5개를 유지해 1행 5개·2행 5개 대칭 그리드가 됐다.
// 2) macroIndicatorCache/macroDetailSnapshot에 달러인덱스 현재값·등락률이 함께 채워지도록 하고, 지표
//    상세 팝업(개념·관전포인트)과 최근 동향 해설 문구도 추가했다.
// 3) "시장 종합 평가"에 강달러(달러인덱스 급등 - 원/달러 환율 압박·안전자산 선호)/약달러(하락 - 위험자산
//    선호 심리 회복) 규칙을 추가해, 달러인덱스 흐름이 종합 평가 코멘트에 자연스럽게 녹아들도록 했다.
// 1) 포트폴리오 구성 탭에서 "목표 항목에 해당하지 않는 자산" 안내문(부동산 등 제외 안내)을 삭제했다.
// 2) 전량 매도됐거나(수량 0) 가격 미입력으로 평가금액이 0원인 "유령 자산"이 리밸런싱 실행 가이드/엑셀
//    다운로드에 계속 나타나던 버그를 고쳤다 - 수량이 아니라 실제 평가금액(calcRow().curAmount)이 0인
//    자산을 걸러야 두 경우(전량매도/미보유 수동종목) 다 잡힌다.
// 3) 탭을 전환했다 돌아오면 모든 아코디언이 접힌 상태로 초기화돼야 하는데, 새로 추가된 "시나리오별
//    일반계좌/총자산 금액 비교" 아코디언 키가 초기화 목록에서 빠져 있던 버그를 고쳤다(키를 일일이
//    나열하지 않고 전체 순회하도록 일반화해 앞으로 키가 늘어도 다시 고칠 필요가 없다).
// 4) 절세계좌 "적립 예상" 버튼/소유자 드롭다운에 44px 최소 터치 영역(touch-target)을 적용했다 -
//    모바일 실측 폭에서 30px 안팎으로 렌더링돼 손가락으로 누르면 옆 요소와 자꾸 오탭되던 문제 수정.
// 5) 미래예측 탭 카드 문구에서 "리밸런싱" 표현을 걷어내고("목표배분·보수적/일반적/긍정적" 등 자연스러운
//    용어로 정리), "시나리오별 적용 수익률 요약" 카드를 삭제했다(세부 수익률은 [수익률 관리] 팝업에서
//    확인). 카드 상단에 "일반계좌 현황" 섹션 타이틀을 추가했다.
// 6) 미래예측 시뮬레이션 기간을 30년→20년으로 단축했다(5/10/15/20년 마일스톤만 표시).
// 1) '리밸런싱 설정' 탭을 '포트폴리오 구성'으로 개명하고, 이 탭 + 미래예측 탭 모두 절세계좌(ISA/IRP/
//    연금저축) 및 부동산을 완전히 제외한 순수 일반계좌 자산만 대상으로 계산하도록 통일했다(이전엔
//    미래예측만 절세계좌+부동산까지 포함했었음 - 다시 되돌림).
// 2) 새 "절세계좌 현황" 카드: 신랑/와이프 드롭다운으로 절세계좌 합산 현황을 보고, [적립 예상] 팝업에서
//    월 적립액+기간을 입력하면 위험자산70%/안전자산30% 고정 비율로 복리 성장한 보수/일반/긍정 3개
//    시나리오 예상액을 계산한다.
// 3) '리밸런싱 후(일반적)-상품비중' 카드를 '시나리오별 적용 수익률 요약'보다 위로 재배치.
// 4) '3가지 시나리오 미래 자산 통합 비교'→'시나리오별 일반계좌 그래프', '시나리오별 예상 자산 스케줄
//    비교'→'시나리오별 일반계좌 금액 비교'(아코디언 접기/펼치기로 전환)로 개명.
// 5) 신규 "시나리오별 총 자산 그래프"/"시나리오별 총자산 금액 비교" 카드 추가 - 일반계좌 시나리오에
//    절세계좌 적립 계획과 부동산(현재가치 복리 성장)까지 더한 가구 전체 총자산 기준.
// 1) VIX/코스피/코스닥/S&P500/나스닥/다우/美10년물 지수 상세 팝업 차트가 "해외 티커"라는 이유만으로
//    가격도 아닌 지수(포인트)값에 "$"를 붙이고 있던 버그를 고쳤다(isIndexPoint 플래그 도입).
// 2) "시장현황 & 매크로 브리핑"에 금 시세(Gold, Yahoo GC=F)를 새로 추가하고 1행 4개(VIX/원달러/
//    美10년물/금시세)·2행 5개(코스피/코스닥/S&P500/나스닥/다우) 레이아웃으로 재구성했다. "시장 종합
//    평가"에도 "금 급등+VIX 높음=안전자산 선호" 규칙을 추가해 금시세 변동이 코멘트에 반영되게 했다.
// 3) '일간금융평가손익'·'총금융자산평가손익'·'총자산평가금액'·'총자산투자금액' 카드 타이틀의 글자
//    크기/굵기를 '기간별 실현 손익' 카드와 동일한 text-sm font-semibold로 통일했다.
// '목표비중 설정'과 '실행 가이드' 서브탭을 "리밸런싱 설정" 하나로 합쳤다. 상단 "리밸런싱 효과 요약"
// 카드와 하단 "국내/해외 리밸런싱 결과"·"국내/해외 세부 리밸런싱 결과" 아코디언 카드 3개를 완전히
// 삭제하고, 그 자리에 기존 '실행 가이드'의 "종목별 리밸런싱 실행 가이드"를 그대로 옮겨왔다(목표비중
// 미달 자산 안내문도 함께 이동). 이 카드들만 채우던 JS 렌더 함수·아코디언 상태 코드·미래예측의
// "현재 구성 유지" 시뮬레이션 계산까지 전부 정리해 죽은 코드가 남지 않게 했다.
// 1) 상단 컨트롤 바(환율뱃지·다크모드·서버동기화): 이 셋만 별도 flex-nowrap 그룹으로 묶어 justify-between
//    + w-full로 어떤 모바일 화면 폭에서도 절대 줄바꿈 없이 한 줄에 균등 분산 배치되게 했다(내용이 화면
//    보다 넓어지는 극단적으로 좁은 기기에서는 그 줄 안에서만 가로 스크롤). 바깥 컨테이너는 그대로
//    flex-wrap을 유지해 아래 6버튼 그리드는 예전처럼 정상적으로 다음 줄로 넘어간다.
// 2) 야간/주간 모드 전환 깜박임 제거: 토글 순간 html에 .theme-switching을 붙여 모든 transition을 꺼서
//    색이 한 프레임에 즉시 바뀌게 하고, 다음 프레임에 다시 켜서 테마가 여러 요소에 걸쳐 각자 다른
//    타이밍으로 교차 페이드되며 생기던 깜박임을 없앴다.
// 3) 핵심종목 실시간 팝업 부팅 자동 오픈 완전 제거 - 이제 헤더의 [핵심종목 실시간] 버튼을 직접 눌렀을
//    때만 열린다.
// 4) '수익율 관리' 버튼을 '월 적립금' 옆으로 옮기고 브랜드 색으로 시인성을 높였다.
// 5) 미래자산 비교를 '현재 구성 유지' 포함 4가지에서 리밸런싱 후 보수적/일반적/긍정적 3가지로 개편
//    (요약 카드·통합 비교 차트·스케줄 표 전부) - '리밸런싱 효과 요약' 카드(현재유지 vs 일반적 차액)는
//    별개 영역이라 그대로 유지된다.
// 6) 탭 전환 시 펼쳐둔 아코디언/드롭다운을 상위·하위 탭 어디서든 항상 접힌 상태로 초기화하고, 스크롤
//    위치도 항상 맨 위로 되돌리도록 switchTab()/switchRebalanceSubTab()을 고쳤다.
// 1) 모바일 "서버 동기화" 버튼: v158의 고정폭(92px) 방식이 실제 기기에서는 오히려 "동기화중"까지
//    줄바뀜을 유발해, 대신 환율 배지의 연필 아이콘·"환율보기" 텍스트를 모바일에서 숨겨 공간을 확보하고
//    동기화 버튼은 원래 크기(고정폭 없음)로 되돌려 항상 환율/야간모드 버튼과 같은 줄에 위치하게 했다.
// 2) 지수 상세 팝업 통일: "시장 현황 & 매크로 브리핑"의 지수/지표 타일 클릭 시 뜨던 전용 #macroDetailModal을
//    완전히 제거하고, "핵심종목 실시간" 팝업이 쓰는 종목 상세 모달(#assetDetailModal)을 그대로 열도록
//    통일했다 - 목록에 없던 지표(VIX, 환율 등)도 동일한 크기/스타일로 동적 렌더링된다.
// 3) [다크모드 배경색 버그 수정 - 진짜 원인] '서버 동기화중' 버튼, 'JSON 자동 백업중' 버튼, '종목 분석
//    & 투자 검토 보고서' 버튼이 야간 모드에서 흰색/밝은 배경으로 보이던 문제의 실제 원인을 찾았다:
//    이 버튼들이 쓰는 dark:bg-brand-950 계열 클래스가 참조하는 'brand-950' 색상 자체가 tailwind.config의
//    brand 팔레트에 정의돼 있지 않아(900까지만 존재) Tailwind CDN JIT이 해당 CSS를 아예 생성하지 못했다
//    - 팔레트에 950 단계(#1e1b4b)를 추가해 근본 원인을 해결했다. 부수적으로 서버 동기화 상태 갱신 함수
//    (updateSyncStatusUI)가 상태가 안 바뀌어도 매번 색상 클래스를 지웠다 다시 붙이던 것도, 실제로 상태가
//    바뀔 때만 건드리도록 고쳐 배경색 전환 애니메이션이 불필요하게 재시작되지 않게 했다.
// index.html·js/03·08·10·12가 바뀌었으므로 v158->v159로 올려 PWA가 캐시된 예전 버전을 버리고 새로
// 받아오게 한다.
const APP_SHELL = [
  './',
  './index.html',
  './manifest.json',
  './js/01-core-state.js',
  './js/02-dashboard-kpi.js',
  './js/03-filters-charts-tabs.js',
  './js/04-rebalancing.js',
  './js/05-future-projection.js',
  './js/06-transactions.js',
  './js/07-table-render-modals.js',
  './js/08-detail-modal-fx.js',
  './js/09-price-fx-risk-engine.js',
  './js/10-risk-translation-alerts.js',
  './js/11-refresh-history.js',
  './js/12-import-export-sync.js',
  './js/13-fundamental-data.js',
  './js/14-settings-boot.js'
];

// 외부 시세/환율 API 및 CORS 프록시는 항상 최신 데이터가 우선이므로 네트워크를 먼저 시도하고,
// 오프라인일 때만 마지막으로 캐시된 응답을 폴백으로 사용한다.
// [버그 수정] polling.finance.naver.com(국내 장전/장후 시간외 시세의 핵심 소스)과 프록시 2개
// (api.codetabs.com, r.jina.ai)가 이 목록에 빠져 있었다 - 그 결과 PWA로 설치된 모바일에서는
// 서비스워커가 이 요청들을 "캐시 우선"으로 처리해, 최초 1회 조회 이후로는 계속 그때 캐시된 옛
// 시세만 반환되고 실제 새 시간외 시세가 전혀 반영되지 않는 문제가 있었다(file:// 로컬 실행에서는
// 서비스워커 자체가 등록되지 않아 이 버그가 재현되지 않았던 것도 원인 파악이 늦어진 이유다).
const NETWORK_FIRST_HOSTS = [
  'query1.finance.yahoo.com',
  'open.er-api.com',
  'api.exchangerate-api.com',
  'stooq.com',
  'api.allorigins.win',
  'corsproxy.io',
  'api.codetabs.com',
  'r.jina.ai',
  'polling.finance.naver.com',
  'asset-manager-proxy.key4125.workers.dev',
  // [가족 동기화] 동기화 데이터는 항상 최신이어야 하므로 캐시에 절대 의존하면 안 된다.
  'steep-haze-01f0.key4125.workers.dev',
  // [한국투자증권(KIS) 재무/수급 프록시] 시세/재무 데이터도 항상 최신이어야 하므로 동일하게 취급한다.
  'keymaster.key4125.workers.dev',
  // [종목 마스터 데이터] data/ticker-master.json은 js/09 loadTickerMaster()가 이미 localStorage에
  // 자체적인 20일 캐시/TTL을 두고 있으므로, 서비스워커까지 이중으로 오래 캐싱해두면 그 로직이 최신
  // 데이터를 원해도 계속 옛 응답만 받게 될 수 있다 - 항상 네트워크를 먼저 시도하게 한다.
  'cdn.jsdelivr.net'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then((cache) => cache.addAll(APP_SHELL))
      .catch(() => {}) // 앱 셸 캐싱 실패해도 설치 자체는 계속 진행
  );
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((keys) =>
      Promise.all(keys.filter((k) => k !== CACHE_NAME).map((k) => caches.delete(k)))
    )
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  const req = event.request;
  if (req.method !== 'GET') return;

  let isNetworkFirst = false;
  try {
    const host = new URL(req.url).hostname;
    isNetworkFirst = NETWORK_FIRST_HOSTS.some((h) => host.includes(h));
  } catch (e) {
    return; // chrome-extension:// 등 파싱 불가한 요청은 그대로 통과
  }

  if (isNetworkFirst) {
    event.respondWith(
      fetch(req).catch(() => caches.match(req))
    );
    return;
  }

  // 앱 셸/정적 리소스(Tailwind, Chart.js 등 CDN 포함): 캐시 우선, 없으면 네트워크 후 캐시에 저장
  event.respondWith(
    caches.match(req).then((cached) => {
      if (cached) return cached;
      return fetch(req)
        .then((res) => {
          const resClone = res.clone();
          caches.open(CACHE_NAME).then((cache) => cache.put(req, resClone)).catch(() => {});
          return res;
        })
        .catch(() => cached);
    })
  );
});
